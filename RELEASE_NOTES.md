# cghostty 0.2.6

本次更新减少命令面板、分屏、拖拽、链接高亮、搜索和无障碍读取中的重复工作，
并完善相关缓存的失效处理与回归验证。构建号为 16。

## 交互与性能

- 命令面板预计算排序标题，使用稳定条目标识，并复用搜索结果和文字高亮索引。
  候选内容变化时重新计算，缓存命中时仍刷新操作回调。
- 分屏树改为按需遍历，避免访问单个元素时反复生成全部叶节点数组；
  窗口登记使用对象身份集合检查成员变化。
- 拖拽预览直接生成目标尺寸位图，保留 Retina 清晰度，避免先截图整个终端再缩小。
  拖拽载荷直接写入终端 UUID，移除异步转同步等待。
- 静态视口内移动鼠标时复用链接正则匹配结果；内容、布局、配置等变化时重建。
  匹配工作区按遍历复用，原有正则执行预算继续保留。
- 连续搜索输入合并为同批最后一个查询，减少已过期搜索的初始化；
  上一个／下一个匹配操作保留查询与导航的执行顺序。
- 快捷指令缩略图按终端缓存，完成帧、视图尺寸或显示缩放变化时重新生成；
  只读取终端元数据时不生成图片。

## 无障碍与维护

- 无障碍文本、可见范围和选区范围在同一次锁定中采集，统一使用 UTF-16 偏移，
  覆盖中文、emoji、组合字符、软换行和矩形选区。
- 内容与范围未变化时复用不可变快照和行索引，避免重复扫描滚动历史。
  修改检测独立于渲染器脏标志；持续输出或范围变化时仍会重新采集。
- 核心构建指纹排除依赖下载缓存，避免缓存内容变化导致无效重建。
- 精简进程等待接口，删除未使用的忙等分支。
- 增加缓存失效、快照生命周期、分屏遍历及搜索顺序回归，补充可复现的局部性能基准。

## 验证

- 最新优化回归：核心定向测试 79/79 通过；原生测试 336 项，335 通过、1 项既有示例跳过、0 失败；
  Python 构建维护测试 24/24 通过。
- 详细测量和验证边界见 [第二批优化验证](docs/validation/2026-09-24-repeated-work.md)
  及 [第一批优化验证](docs/validation/2026-09-24-six-improvements.md)。
- 上述构建和运行验证在升版前的 0.2.5 元数据下完成；0.2.6 已同步版本信息并检查一致性，
  尚未单独构建发行包或完成人工 VoiceOver、真实终端拖拽验收。

现有配置无需因本次更新调整。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
