# cghostty 0.2.9

缩短光标主体移动时间，让长距离跳转更快到达。
构建号为 19。

## 光标动画

- 主体移动时间上限从 220ms 调整为 200ms，仍按距离变化。
- 单格移动保持 24ms，保留长距离起步加速与平滑减速。
- 尾部历史窗口保持 40–60ms，形状恢复时间保持 100ms。
- 最大放大幅度保持 12%；横向、竖向和斜向统一生效。

## 验证

- 动画改动的渲染器回归 144/144 通过，覆盖持续输入、快速转向、
  多刷新率轨迹连续性和 Vim 光标形状切换。
  测试在版本递增前的 0.2.8 元数据下完成。
- 尚未构建 0.2.9 应用或发行安装包。

现有配置无需调整。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
