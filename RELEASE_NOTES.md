# cghostty 0.2.7

本次更新适配 Unicode 18，改进同步输出的完整帧显示，并减少渲染、搜索和原生调度中的重复工作。
构建号为 17。

## 终端兼容性

- 升级 uucode 依赖并适配 Unicode 18 字素边界规则，保留本地预计算和字符宽度策略。
- 终端模式查找使用编译期排序集合与二分查找，保留现有模式清单、默认值及报告行为，
  并拒绝超出有效范围的模式编号。
- 同步输出（mode 2026）保留此前完成的视口，避免同一次输入中结束旧帧、开始新帧时遗漏完整画面。
  文本、样式、链接和 Kitty 图片随快照保持一致；重复开启、超时、重置及尺寸变化继续正确处理。

## 性能与维护

- 同步输出复用 CPU 行数据、样式容器和同代图片像素，减少反复分配与复制。
  待显示完整帧最多保留一份，图片缓存限定于最近采集的视口。
- Kitty 图片放置数据复用各交换链帧的 Metal 缓冲区，保持原有绘制与透明混合顺序。
- 搜索在输出、视口或可见性变化后合并刷新，静态会话停止周期性轮询；
  隐藏期间保留更新状态，重新显示时补充刷新，显式查询与导航仍立即处理。
- 合并突发主线程唤醒，同时处理退出或错误后队列中仍有消息的情况。
- 使用 UUID 弱索引查找终端视图，减少分屏树扫描，并覆盖关闭、跨窗口移动和撤销恢复。
- 统一 Metal 缓冲区扩容与写入逻辑，先成功分配再替换旧资源，检查长度溢出，
  并按 Apple Silicon 平台范围精简存储模式与同步分支。

## 验证

- Unicode、终端模式及同步输出适配：核心定向测试 165/165 通过。
- 最新优化回归：核心定向测试 189/189 通过；原生测试 340 项，339 通过、
  1 项既有基准示例跳过、0 失败，包含实际 PTY 搜索及 Kitty 图片渲染像素验证。
- 修改文件的格式及严格 Swift lint、平台范围、版本、本地化和 Swift 6 配置检查通过。
- 详细验证、局部性能测量及复现方法见 [终端适配验证](docs/validation/2026-09-24-unicode18-render-hold.md)
  和 [渲染与搜索优化验证](docs/validation/2026-09-24-render-search-reuse.md)。
- 上述构建、运行测试和性能测量在 0.2.6（build 16）元数据下完成。
  0.2.7 已同步版本信息并检查一致性，尚未单独构建发行包，也未运行完整核心测试套件。

现有配置无需因本次更新调整。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
