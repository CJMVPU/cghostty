# cghostty 0.3.8

减少渲染与输入中的重复工作，缩小动画光标文字覆盖层的绘制范围，
并整理终端核心的实现与测试边界。构建号为 28。

## 渲染与输入

- 前景文字按行增量上传，光标闪烁不再移动并重新上传全部文字。
- 滚动内容纹理按实际内容变化失效，避免单独的光标变化触发重建。
- 动画方块光标的文字覆盖层只提交可能相交的行，并限制 Metal 裁剪范围；
  动画竖线和下划线省去不需要的文字覆盖层。
- 绘制范围考虑宽字符、字形越界、组合字符和滚动偏移，原生光标保留原有路径。
- 缓存同一逻辑行的链接匹配与未命中结果，减少重复悬停计算和原生通知。

## 代码与测试维护

- 分离 Screen、Terminal、PageList 的全部 939 个命名测试，按行为主题组织，
  保留组件过滤器和仅用于测试的内部访问入口。
- 提取选择范围计算和 Pin 遍历；页面、视口及跟踪坐标的状态仍由原组件管理。
- 集中管理键盘序列状态、滚动场景资源，以及批量关闭标签页的确认与撤销逻辑。
- 更新真实桌面回归的配置与状态采样，覆盖缓存刷新、光标运动和滚动区域。

## 选词与光标修复

- 修复双击宽字符时只选中一个字或无法选中的问题：汉字左右两半、软换行
  占位格都解析到实际字符，选词仍在硬换行处停止。
- 默认选词边界加入常用中文标点、引号、括号和全角空格；例如“你好，世界”
  可分别选中“你好”或“世界”。自定义 `selection-word-chars` 仍替换默认边界。
- 修复反向换行模式下恢复待换行光标后，左移或退格可能跳到滚动区域顶部的问题。

## 验证与性能观察

- 上述功能在 0.3.7／构建 27 下完成 1,176 项定向 Zig 回归、349 项原生测试，
  以及 4 项光标和 3 项滚动 UI 回归，均通过。
- 同环境短时测量中，覆盖层提交实例在持续输入、长距离跳转和分屏场景中
  分别减少约 95%、69%、35%；这是提交工作量，不能等同于整体性能提升。
- 输入和跳转观察到较低 GPU 平均时间；分屏耗时仍有波动，部分 trace 存在
  少量记录丢失，尚不能据此宣称所有场景都有稳定提速。
- 完整验证与测量限制见 [测试分离与光标覆盖层记录](docs/validation/2026-09-27-test-separation-overlay.md)
  和 [渲染缓存、输入与标签页记录](docs/validation/2026-09-27-render-input-refactor.md)。
- 选词与反向换行修复前复现 4 项失败；修复后的 110 项定向回归通过，详见
  [选词与反向换行记录](docs/validation/2026-09-27-word-selection-wrap.md)。
- 0.3.8 尚未构建原生应用或生成发行包；上述新增修复已完成核心定向测试。

支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
