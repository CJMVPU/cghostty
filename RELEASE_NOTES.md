# cghostty 0.1.4

支持 **macOS 27 及以上版本、Apple Silicon（arm64）**。

## 更新内容

- 以前一帧实际显示位置判断移动方向，避免尚未追上旧目标时，新目标的位置导致前后端判反。
- 光标动画改为方向驱动的四角运动：横向由左右边领跑和跟随，竖向由上下边领跑和跟随；斜向由最前方的角领跑，两侧角逐渐过渡，对角跟随。
- 前沿先轻微展开、到位后恢复，轴向移动的展开幅度上限约为 15%。持续输入不会叠加展开幅度；快速反向保持当前角点位置连续，并绘制凸轮廓，避免交叉翻折。
- 修复 Vim 连续搜索时动画被临时隐藏光标打断的问题。Vim 重绘时仍尊重隐藏指令，但保留运动状态，在下一个可见位置继续动画。
- 连续按键使用始终不同的前后响应时间，并从当前显示位置接续，避免反复起步或前后延迟差归零后拖尾消失。
- 保留 Vim 插入模式的细线、替换模式的下划线光标，原有厚度不参与展开；细线按字符格宽度计算动画时长。模式或尺寸变化时重置几何，后续输入继续动画。

## 配置

默认启用 `cursor-effect = smooth`。已有配置无需修改；可用 `cursor-effect = none` 关闭。系统启用“减少动态效果”时仍停用动画。

## 验证

- 光标专项测试 10/10 通过；渲染器与配置回归 296/296 通过。
- 覆盖八个方向、前沿展开与恢复、每 8/16/33ms 连续输入 500 次、搜索位置循环与隐藏/显示交替、细线模式切换，以及随机快速反向时的凸轮廓。
- macOS 自带 Vim 的 PTY 输出验证：连续搜索 100 次，100 次均发送隐藏/显示光标指令，确认此次修复对应的真实重绘路径。

- ReleaseLocal 0.1.4（4）构建、macOS/arm64 范围、资源、签名和 ZIP 完整性检查通过。
- 独立测试窗口中，Vim 定时执行 1,200 次搜索跳转；运行画面确认光标拉伸和插入模式细线，Metal 校验未报告错误。此项不是物理键盘长按或所有帧的像素分析。

## 安装与校验

下载 `cghostty-0.1.4-macos-arm64.zip`，解压后将 `cghostty.app` 放入“应用程序”。将 ZIP 与 `.sha256` 文件放在同一目录后执行：

```sh
shasum -a 256 -c cghostty-0.1.4-macos-arm64.zip.sha256
```

此发行包使用 **ad-hoc 签名**，未做 Developer ID 签名或 Apple 公证。首次打开可能受到 Gatekeeper 限制；确认来源后，可在系统设置“隐私与安全性”中允许打开。

应用内“检查更新”打开本仓库 Releases 页面，需下载并替换应用。
