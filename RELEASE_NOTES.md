# cghostty 0.3.1

修复光标跳到中文宽字符或切换形状时移动动画突然消失的问题。
构建号为 21。

## 光标动画

- 一格与两格字符之间切换时，保留当前位置、运动过程和拖尾，平滑过渡到新宽度。
- 方块、竖线与下划线之间切换时，尺寸、形状及文字变色效果连续过渡。
- 字体缩放和窗口尺寸变化不再直接清空运动状态，光标从当前屏幕位置继续移动到新布局下的目标。
- 宽度与形状采用独立的 100ms 过渡，保留现有位置移动节奏；同一目标上的形状变化不会反复重启移动计时。
- 本次未调整内容滚动而光标屏幕位置不变时的表现。

## 验证

- 核心定向回归 93/93 通过，桌面 Metal 回归四个场景最终通过，覆盖中文宽字符、形状切换、VSync 开关、拖尾及闪烁。
- 上述测试与 ReleaseLocal 构建在提升版本前的 0.3.0／20 下完成；尚未重新构建 0.3.1 应用或发行包。
- 详细过程见 [光标连续过渡验证](docs/validation/2026-09-26-cursor-geometry-continuity.md)。

现有配置无需调整；Claude 兼容模式继续通过 `claude-compatibility = true` 手动开启。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
