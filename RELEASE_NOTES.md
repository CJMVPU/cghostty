# cghostty 0.3.2

优化同步输出（mode 2026）的逐帧开销，并加入终端平滑滚动。构建号为 22。

## 同步输出

- 同步快照与实时渲染共同消费脏行；稳定视口只复制变化行，不再在每次进入和退出同步时强制整屏刷新。
- GUI 直接接管变化行的存储，保留未变化行的文字与渲染缓存；样式展开仍在渲染线程执行。
- 连续同步帧合并到一个有界待处理快照，保留同一次 PTY 输入内退出并立即重新进入同步的完整帧边界。
- 分配失败后恢复完整刷新；窗口尺寸、屏幕切换和实际全屏变化仍按需要完整更新。

## 平滑滚动

- 新增默认开启的 `smooth-scroll = true`，配置模板包含中英文说明。
- 终端历史滚动支持触摸板像素偏移和滚轮过渡；Neovim 等 TUI 的实际滚动区域独立过渡，状态栏保持固定。
- 动画从最后显示的画面继续，选择与链接命中位置跟随视觉偏移。
- 每个可见终端最多按需缓存三张内容纹理；减少动态效果设置及文字上层 Kitty 图片使用普通渲染。

## 验证

0.3.2 / 22 的 ReleaseLocal 应用已成功构建，最终核心定向回归 **231/231**、桌面回归 **5/5** 通过，版本一致性、范围与格式检查通过。

另修复原生搜索测试的 PTY 启动竞态，等待子进程就绪后再输入，避免登录提示插入测试数据。与 CI 相同的 Debug 原生套件 **346 项测试、45 个测试组全部通过**；见 [CI 搜索测试验证](docs/validation/2026-09-26-ci-search-pty-readiness.md)。

核心逐格对照、同步帧边界、中文与组合字符、样式、OSC8 链接、Kitty 图片、尺寸变化和失败恢复均包含定向回归。性能与桌面测试结果见 [mode 2026 验证](docs/validation/2026-09-26-mode2026-incremental.md)；平滑滚动设计见 [平滑滚动验证](docs/validation/2026-09-26-smooth-scroll.md)。

Claude 兼容模式继续通过 `claude-compatibility = true` 手动开启。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
