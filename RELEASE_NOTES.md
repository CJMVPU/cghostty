# cghostty 0.3.5

新增三种可选光标动画方案，并改善 Vim 连续移动时的追赶与转向。
构建号为 25。

## 光标动画方案

- 保留 `cursor-effect = true/false` 总开关，新增 `cursor-effect-mode`。
- `classic` 为默认方案：单次移动 24～200ms，拖尾 40～60ms，膨胀恢复 100ms。
- `responsive`：单次移动 24～160ms，拖尾固定 40ms，膨胀恢复 80ms。
- `instant`：主体立即落在真实位置，保留最大 12% 膨胀，尾迹在 40ms 内淡出，
  最高不透明度为 35%，膨胀恢复 80ms。远距离跳转不限制尾迹连接长度，
  频繁折返不会提前清除时间窗口内的旧尾迹。
- 配置模板和 README 补充三种方案的说明与示例；保存配置后重启应用生效。

## 连续移动优化

- `classic` 和 `responsive` 在目标更新间隔不超过 120ms 时逐渐加速，
  约 300ms 内提高至最高 2.5 倍追赶倍率，主体移动时长最低仍为 24ms。
- 长跳在充分加速后分别可缩短至约 80ms 和 64ms；
  拖尾、膨胀和形状过渡保留各模式的设置。
- 横纵方向分别处理转向：例如 Vim 在长短行间上下移动时，
  横向折返不会清掉仍然正确的纵向速度，并约束各轴避免越过目标。
- 松键后保留最后一段的速度衔接和到达时间，平滑减速到位；
  下一次独立跳转恢复正常时长。普通重绘不会触发加速。
- `instant` 不使用连续加速机制，主体始终跟随最新位置。

## 验证

- 本次功能在 0.3.4／构建 24 元数据下完成定向回归，184/184 通过。
  覆盖配置与模板、连续加速、分轴反转、松键收尾、即时定位、尾迹淡出及生命周期。
- 同一功能状态的 ReleaseLocal 应用构建成功，架构、资源和签名检查通过。
- 本次提升至 0.3.5／构建 25 仅更新版本元数据和发布说明；
  尚未以新版本重新构建应用或生成发行安装包。
- 尚未进行真实 Vim 中长按上下键的视觉手感验收。

现有配置无需调整；未指定新选项时使用 `classic`，并自动启用连续移动优化。
支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
