# cghostty 0.1.3

支持 **macOS 27 及以上版本、Apple Silicon（arm64）**。

## 更新内容

- 重构原生标签栏布局：集中管理并复用布局约束，在标签移交、关闭和临时零尺寸阶段及时释放，消除移动标签时的 AppKit 约束冲突。
- 标签排序直接使用原生窗口组的插入操作，保留选中标签与终端会话；修复切换标签时高度变化，以及窗口标题和分屏缩放按钮的对齐问题。
- 修复终端句柄晚于所属核心释放时的生命周期风险，确保所有终端句柄销毁后才释放核心。
- 内部窗口、标签、分屏和命令面板操作改为类型明确的控制器调用，移除 19 组通知及字典参数，保留快速终端的独立行为。
- 精简无调用的辅助代码、只有一个分支的终端 IO 包装，以及重复的配置诊断和文件路径选择逻辑。
- 使用确认后的暗银纹理圆角图标，光照来自右上方；移除旧的备用图标、自定义图层合成、Dock 插件及相关资源和构建步骤。

## 配置变更

应用统一使用内置图标。旧的 `macos-icon`、`macos-custom-icon`、`macos-icon-frame`、`macos-icon-ghost-color`、`macos-icon-screen-color` 配置已删除；如果个人配置中有这些选项，请删除对应行。

## 安装与校验

下载 `cghostty-0.1.3-macos-arm64.zip`，解压后将 `cghostty.app` 放入“应用程序”。

同目录下载 `.sha256` 文件后执行：

```sh
shasum -a 256 -c cghostty-0.1.3-macos-arm64.zip.sha256
```

此发行包使用 **ad-hoc 签名**，未做 Developer ID 签名或 Apple 公证。首次打开可能受到 Gatekeeper 限制；确认来源后，可在系统设置“隐私与安全性”中允许打开。

## 验证记录

- 原生测试：243 项通过、1 项跳过、0 项失败，运行时警告为 0。
- 桌面交互和布局测试：7/7 通过，运行时警告为 0；覆盖终端输入、搜索、分屏、命令面板、标签反复排序、关闭、全屏、跨窗口拖动与合并。
- 核心相关回归：1,361 项通过、1 项跳过；最终 IO 定向复验 133/133 通过。
- 严格 SwiftLint、Zig 格式、依赖版本记录和 Swift 6 配置检查通过。

详细验证记录和适用边界见 `VALIDATION.md`。
