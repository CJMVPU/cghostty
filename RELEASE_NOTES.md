# cghostty 0.1.2

支持 **macOS 27 及以上版本、Apple Silicon（arm64）**。

## 更新内容

- 统一原生 UI 状态为 Observation，SwiftUI 负责界面，AppKit 集中处理窗口、输入与系统交互；保留终端核心桥接。
- 修复标签切换时窗口控制器生命周期问题，以及核心销毁期间已排队的唤醒回调访问释放对象的问题。
- 使用 PCRE2 10.48 替换 Oniguruma；简单 tmux 协议直接解析，移除旧引擎封装和构建依赖。
- 精简未使用的平台、国际化辅助代码、分发层与彩蛋资源；保留 macOS/arm64、键盘、应用资源及签名检查。
- 完善 CI 环境报告和依赖版本记录，修复 Dear ImGui dSYM 同名归档成员导致的符号警告。
- 测试应用及 runner 默认在系统临时目录运行，测试使用独立配置和工作目录，避免因为源码位于文稿目录而触发不必要的访问请求。
- 为原有小幽灵应用图标增加银色边框，同步应用与 Dock 资源。

## 关于文稿目录权限

应用不会为了普通启动主动申请文稿权限。终端内的命令实际读取文稿中的项目时，仍受 macOS 隐私权限管理；本版本不会绕过系统规则。若不希望授予文稿访问权限，请将项目放到 `~/Developer` 等非受保护目录。

## 安装与校验

下载 `cghostty-0.1.2-macos-arm64.zip`，解压后将 `cghostty.app` 放入“应用程序”。

同目录下载 `.sha256` 文件后执行：

```sh
shasum -a 256 -c cghostty-0.1.2-macos-arm64.zip.sha256
```

此发行包使用 **ad-hoc 签名**，未做 Developer ID 签名或 Apple 公证。首次打开可能受到 Gatekeeper 限制；确认来源后，可在系统设置“隐私与安全性”中允许打开。

## 验证记录

- 原生测试：249 项通过、1 项跳过、0 项失败；桌面交互测试 2/2 通过。
- 核心回归：1,364 项通过、1 项跳过；PCRE2 绑定 3/3 通过。
- 严格 SwiftLint、Zig 格式、版本记录、Swift 6 和工作流检查通过。
- 完整测试期间未记录到新的文稿目录权限请求。
- ReleaseLocal 构建、macOS/arm64 范围、应用资源和签名检查通过。

详细结果和验证边界见 `VALIDATION.md`。
