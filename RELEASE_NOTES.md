# cghostty 0.1.9

本次更新集中改善窗口、分屏与终端会话的管理，精简原生界面和内部构建，
并修复文本编辑与桌面测试中的线程优先级等待问题。

## 窗口与会话

- 统一普通窗口与快捷终端的应用归属、窗口查找和配置更新，
  减少多窗口操作中的重复判断。
- 集中管理分屏移动、关闭、焦点切换与撤销规则，
  跨窗口移动和关闭撤销继续保留原始终端会话。
- 修复撤销记录释放时重入撤销管理器引起的崩溃。
- 窗口恢复使用独立的布局与会话数据快照，
  读取保存数据时不再提前创建窗口或启动终端进程。
  保持已有窗口恢复格式兼容，并修复快捷终端恢复时的专用环境变量继承。
- 集中管理终端 IO 的启动、停止与资源释放，
  完善启动失败、重复停止和关闭时的清理流程。

## 界面与输入

- 移除剩余 XIB，使用 SwiftUI 界面、Observation 状态和集中的 AppKit 系统交互。
  原生标签页、菜单响应链、配置快捷键和窗口恢复继续保留。
- 统一菜单动作与快捷键的登记和更新，减少重复配置。
- 完善搜索视图替换时的焦点归属，以及滚动条、检查器的原生视图引用管理。
- 搜索、命令和标题字段按原始文字编辑，关闭不需要的自动文本检查、
  数据识别和文字替换，避免命令输入框退出焦点时等待系统文本识别器。
  终端输入法组合文本及按键事件处理顺序保持不变。

## 内部精简与验证

- 渲染器收拢为 macOS Metal 专用实现，删除多余后端适配和无消费者的帧导出设施。
- 应用直接链接内部静态库，移除 XCFramework 包装步骤，
  保留实际需要的 Swift／Zig 桥接并删除无调用接口。
- 删除剩余 Combine 订阅及内部自定义通知转发，
  系统通知仅用于仍有需要的原生系统事件。
- 完善恢复数据、会话生命周期、分屏撤销、搜索和命令面板回归。
  修正桌面测试对系统“服务”菜单的不必要扫描，
  已定位的三条线程优先级等待警告在专项复验中消除，线程性能检查保持开启。

## 配置兼容性

以下原本未在 macOS 实现的动作已移除。
如果自定义 `keybind` 仍引用它们，请删除对应配置：

- `toggle_tab_overview`
- `toggle_window_decorations`
- `prompt_window_title`
- `show_gtk_inspector`

支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
