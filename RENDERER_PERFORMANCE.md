# 渲染优化实测（2026-09-21）

主体上限保持 220ms，形变保持 12%，长距离尾部窗口保持 60ms。
同一台 Apple Silicon Mac、ReleaseLocal/ReleaseFast、同一受控 PTY 负载。
每场景预热 1 秒，再观测 6 秒；分别测试垂直同步和定时器绘制。
开启性能记录，未开启 Metal 调试层。测试窗口配置为 140×42 字符、10pt。
分屏场景在动图场景之后新增窗格，包含仍需更新的图像工作。

## 明确的工作量变化

下表 MB 使用十进制。帧数是 CPU 绘制路径次数，不是显示器实际呈现计数。

| 垂直同步场景 | 复制量：优化前 → 后 | 减少 | 光标定时器唤醒：前 → 后 | 绘制次数：前 → 后 |
| --- | --- | --- | --- | --- |
| 静止、不闪烁 | 0.65 → 0 MB | 100% | 0 → 0 | 10 → 0 |
| 持续输入 | 144.15 → 101.81 MB | 29.4% | 730 → 0 | 732 → 729 |
| 长距离跳转 | 144.01 → 21.84 MB | 84.8% | 730 → 0 | 732 → 732 |
| 动图与跳转 | 143.81 → 97.19 MB | 32.4% | 583 → 0 | 731 → 731 |
| 新增分屏 | 140.90 → 57.82 MB | 59.0% | 721 → 0 | 1443 → 1442 |

动图更新定时器仍运行：动图场景 145 → 151 次，分屏场景 149 → 149 次。
关闭垂直同步时，长跳光标定时器为 661 → 657 次，后备绘制正常工作。
该场景复制量减少 84.3%；定时器模式的静止场景也从 10 次绘制降至 0。
持续输入平均尾部段数约 3.56 → 2.01，长跳约 8.00 → 5.15，
分屏约 8.00 → 2.15；转弯仍保留历史路径，不靠截短像素长度减少计算。

## 耗时的实际结果与边界

CPU 列是绘制路径墙钟耗时，包含等待帧槽；不是 CPU 使用率。
GPU 列来自 Metal 4 提交反馈的 GPUStartTime/GPUEndTime。
以下为平均值；原始汇总还包含 p95 和帧间隔分布。

| 垂直同步场景 | CPU 绘制路径：前 → 后 | GPU 执行：前 → 后 |
| --- | --- | --- |
| 持续输入 | 0.211 → 0.246ms | 0.514 → 0.586ms |
| 长距离跳转 | 0.266 → 0.220ms | 0.657 → 0.607ms |
| 动图与跳转 | 0.261 → 0.252ms | 0.581 → 0.551ms |
| 新增分屏 | 0.212 → 0.204ms | 0.890 → 0.675ms |

持续输入的平均耗时没有改善；关闭垂直同步的长跳 CPU 耗时也略增。
因此本轮只确认复制量、无效唤醒和直线尾部段数减少，
不宣称所有场景都更快，也不由这组短测试推算耗电下降比例。
GPU 测量不包含系统最终合成/显示延迟，帧历史记录的是提交位置。
未完成跨显示器、物理 Vim 长按、高速摄像或长期能耗测量。

## 复现与记录

执行入口及导出方法见 `HACKING.md` 的“渲染性能对比”。
场景代码：`macos/GhosttyUITests/GhosttyRendererPerformanceUITests.swift`。
汇总工具：`scripts/summarize-render-trace.py`。

本机结果：

- 优化前：`/private/tmp/cghostty-perf-before-final.xcresult`
- 优化后：`/private/tmp/cghostty-perf-after.xcresult`
- 汇总：`/private/tmp/cghostty-render-performance-comparison.json`

最初几轮修正了测试构建的模块导入、隔离配置和分屏元素名称，
不将这些失败轮次算作性能验收。最终前后两组均完整通过。
