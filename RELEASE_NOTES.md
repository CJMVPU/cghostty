# cghostty 0.3.6

精简配置模板，移除个人调色板覆盖和自定义命令面板条目配置。
构建号为 26。

## 配置模板

- 开头说明按中文、英文分别成段，不再逐行交替。
- 每个配置项保留名称、默认值、示例标题和示例配置四行，枚举选项在默认值同行展示。
- 快捷键完整默认列表放在文末附录，减少正文中的长列表。
- 新增 `cghostty +show-config --template --no-pager`，可导出模板而不改写个人配置。

## 配置功能精简

- 移除个人配置和命令行中的 `palette` 手动颜色覆盖入口；主题仍提供终端配色。
- 移除 `command-palette-entry` 自定义条目、清空条目和恢复条目的配置入口。
- 命令面板及内置操作继续保留，`⌘⇧P` 和 `keybind` 快捷键配置继续可用。
- 模板、配置参考和 shell 补全同步移除上述两个配置入口。
- 仍使用这些配置项的旧个人文件会报告未知字段，升级时需删除相应有效配置行。

## 验证

- 自定义命令面板移除后的定向测试 89/89 通过，覆盖内置条目、原生读取、
  配置入口、模板、文件格式和主题加载。
- 同一功能状态在 0.3.5／构建 25 下完成 ReleaseLocal 构建及应用签名、架构、
  资源和实际配置加载检查。
- 本次版本提升仅更新版本元数据和发布说明；尚未重新构建 0.3.6 应用或生成发行包。

支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
