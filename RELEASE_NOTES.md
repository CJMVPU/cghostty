# cghostty 0.3.4

修复连续搜索跳转时光标突然加速的问题。
构建号为 24。

## 光标动画

- 长跳尚未结束时，新的附近匹配会同时按目标间距和主体剩余距离分配移动时间，
  避免将大段追赶压入 24ms，改善 Vim 持续搜索时的运动衔接。
- 主体移动上限保持 200ms，普通单格输入保持 24ms。
- 尾部窗口保持 40–60ms，形状恢复和几何过渡保持 100ms，最大放大保持 12%。
- 补齐英文单格、中文双格、竖线和下划线之间的组合回归，
  验证移动中切换宽度和形状时的位置与尺寸连续性。

## 性能诊断

- 新建或恢复默认配置时，模板明确写入 `render-trace = false`。
  给现有配置补充说明时仍使用注释，保留用户已有诊断设置。
- 修复性能 UI 测试的诊断开关未生效：通过隔离配置开启记录，
  避免 Xcode 环境跳过命令行参数后无法取得数据。
- 本机持续输入、长跳和分屏场景的平均刷新回调间隔约为 8.33ms，
  即约 120Hz；没有新增强制刷新率选项。
  回调和绘制节奏不等于显示器实际呈现帧率。

## 验证

- 渲染器回归 169/169 通过；ReleaseLocal 核心与原生测试应用构建成功。
- 垂直同步性能 UI 测试 1/1 通过，修改的 Swift 文件严格 lint 无违规。
- 以上验证在 0.3.3 元数据下完成，尚未构建 0.3.4 应用或发行安装包。
- 配置模板与既有设置保留的定向回归在 0.3.4 元数据下完成，70/70 通过。
- 性能测试为合成终端负载，未进行真实 Vim 长按 n 的手感验收。
  详细数据与测量边界见 [连续搜索验证](docs/validation/2026-09-26-cursor-retarget.md)。

现有配置无需调整。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
