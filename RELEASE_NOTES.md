# cghostty 0.3.3

修复前后台切换相关的帧提交与跨线程状态问题，增加默认关闭的渲染诊断配置。构建号为 23。

## 渲染稳定性

- 同步重绘与异步 GPU 帧使用统一提交序号，避免同尺寸旧帧覆盖较新的画面。
- 主线程合并待显示帧，只保留最新完成帧；隐藏、关闭和画面缓冲复用时淘汰过期提交。
- 统一焦点、可见性和动画调度状态的锁保护；DisplayLink 的启停保留在绘制锁外。
- 排队显示任务独立管理 layer 与邮箱生命周期，避免 renderer 销毁后访问其诊断状态。

## 可选渲染诊断

- 新增 `render-trace = false`。Debug、ReleaseLocal 和正式 Release 均默认关闭，需明确设置为 `true` 才记录。
- `render-trace-directory` 指定绝对输出目录，默认 `/tmp/cghostty-render-trace`；启用时自动创建。修改配置后重启生效。
- 关闭时不创建日志线程、缓冲区或文件，不采集新增性能时间戳；旧的 `CGHOSTTY_RENDER_TRACE` 环境变量不再启用诊断。
- 开启后使用有界内存缓冲和后台批量写盘。缓冲锁竞争或容量不足时丢弃记录并计数，渲染线程不等待日志写盘。
- 可记录刷新回调间隔、绘制锁等待、资源重建和主线程显示排队时间。记录不包含终端文字或输入；开启诊断仍有额外开销。

## 验证

本次修复的渲染核心回归 **164/164** 通过；原生测试 **346 通过、0 失败、1 项原有示例基准跳过**；配置开关、模板与日志回归 **72/72** 通过，日志汇总脚本 **2/2** 通过。

新增原生用例覆盖真实 Metal 路径下焦点、可见性切换与同步重绘交错。以上验证不等同于电影并行播放时的实际显示帧率测量，偶发掉帧仍需在日常场景观察。

支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
