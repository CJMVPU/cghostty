# cghostty 0.1.8

本次更新改善光标移动的连续性，并减少渲染器的重复工作。

- 长距离移动增加渐进起步，减少起点附近跳跃的感觉。
  主体动画上限为 220ms，单格输入仍保持快速响应。
- 尾部沿近期绘制位置连接，保留转弯和前后帧的移动路径。
  主体保持完整，尾部不设像素长度上限，停止后自然收拢。
- 改善连续输入和快速改向时的速度衔接。
  同方向移动承接当前速度，强反向时限制旧方向惯性。
- 尾部按路径长度渐细，减少刷新率和采样密度对外观的影响。
  合并近直线路径的冗余点，同时保留连接点和转折。
- 光标动画复用未变化的文字与背景数据，减少重复复制。
  本机受控长距离跳转测试中，复制量减少约 85%。
  该数值不代表整体帧率提升或耗电下降比例。
- 垂直同步运行时取消重复的光标定时器唤醒，
  同时保留无垂直同步的后备绘制和 Kitty 动图更新。
- 修复常亮光标静止后仍被闪烁定时器触发重绘的问题。
- 完善 GPU 执行和显示提交失败后的光标状态处理。
- 补充持续输入、方向切换、缓存刷新与闪烁切换回归，
  并增加可重复的渲染性能测试和可选诊断记录。

保留主体等比例放大 12%、轻微椭圆化及默认 3 像素细线。
方块、细线和下划线光标均使用相同的动画规则。

支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
