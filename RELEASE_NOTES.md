# cghostty 0.2.8

本次更新减少渲染与原生界面的重复工作，并改善 CI 中真实 GPU 测试的运行条件和失败诊断。
构建号为 18。

## 渲染与搜索

- 字体图集按变化区域上传，背景单元按变化行上传；各渲染帧分别跟踪版本，
  首次使用、布局变化及失败重试保持正确的全量恢复路径。
- 图片状态未变化时跳过上传表扫描和放置缓冲区重写，虚拟占位符按内容、视口、
  字体尺寸及图片状态变化更新；同步输出快照切换和准备失败均会正确失效。
- 搜索快照共享不可变结果，高亮按页面及代数定位可见行；只有范围变化的行重新标脏，
  保留选中匹配优先级，防止过期结果匹配到复用后的页面。
- 复用有容量上限的渲染与链接查找工作区；GPU 资源替换失败时保留旧资源。

## 原生界面

- 鼠标样式、滚动条外观和尺寸刷新分别处理，合并重复尺寸通知，复用未变化的跟踪区域。
- 字体单元尺寸变化即使不触发布局，也会更新网格尺寸及滚动比例；屏幕缩放变化仍正确通知核心。
- 复用标题栏观察器并按相关事件刷新；内部回调按职责拆分。

## CI 与诊断

- 两项真实 GPU 测试检测显示设备的 Metal 4 能力；不支持时明确跳过，
  声明支持但编译器初始化失败时仍作为失败处理，其余原生测试照常执行。
- 首帧验证先显式设置视口尺寸并产生终端输出，不再依赖空闲光标闪烁。
  GPU 用例串行运行，超时报告帧版本、渲染健康及窗口状态。
- 记录运行时 GPU 名称、Metal 4 能力及编译器创建结果；保留 Metal 返回的底层错误信息。
- 在 CI 摘要中列出具体失败断言与跳过原因，继续上传已有的完整日志和 xcresult。
  因环境能力跳过 GPU 用例不代表已通过 GPU 渲染验证。

## 验证

- 增量渲染改动的核心定向回归：200/200 通过；原生回归：344 通过、1 项既有示例跳过、0 失败。
  这些结果在 0.2.7（build 17）元数据下完成。
- 0.2.8 Debug 核心与原生应用构建成功；原生回归 344 通过、1 项既有示例跳过、0 失败，
  两项真实 GPU 测试均执行通过。脚本回归 26/26 通过，生成的 Debug 应用版本与 build 18 一致。
- 远端修复后的 Actions 尚未运行，未构建发行包；最终证据见 [CI 诊断及版本验证](docs/validation/2026-09-24-ci-metal-0.2.8.md)。
- 工作量计数及边界见 [资源更新验证](docs/validation/2026-09-24-resource-updates.md)
  和 [增量渲染验证](docs/validation/2026-09-24-incremental-rendering.md)。

现有配置无需因本次更新调整。支持 macOS 27+，仅提供 Apple Silicon（arm64）版本。
