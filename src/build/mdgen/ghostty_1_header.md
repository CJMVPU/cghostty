% CGHOSTTY(1) Version @@VERSION@@ | cghostty terminal emulator

# NAME

**cghostty** - Native macOS Apple Silicon terminal emulator

# DESCRIPTION

cghostty is a macOS terminal application based on Ghostty. It uses a Swift
native interface, a Zig terminal core, Metal rendering, and CoreText fonts.
Only macOS on Apple Silicon is supported.
